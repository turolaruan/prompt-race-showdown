version: "3.8"
services:
  web:
    image: node:20
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "npm install --no-audit --silent && npm run dev -- --host 0.0.0.0 --port 80"
    ports:
      - "80:80"
    environment:
      - NODE_ENV=development
    restart: unless-stopped
